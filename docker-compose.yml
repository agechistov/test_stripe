services:
  project:
    build: .
    env_file: .env
    environment:
      - POSTGRES_HOST=db
    depends_on:
      - db
    ports:
      - "8001:8000"
  db:
    image: postgres:18.1
    restart: unless-stopped
    env_file: .env
    ports:
      - "5432:5432"
